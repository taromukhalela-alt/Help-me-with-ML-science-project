"""MVC application modules.

The legacy ``app.py`` remains the composition root while features are moved
here one at a time.  Keeping the package independent of the Flask instance
makes each service straightforward to unit test.
"""
